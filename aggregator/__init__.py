"""
aggregator
==========

Ядро застосунку-агрегатора нерухомості. Складається з невеликих
незалежних частин, кожна у своєму файлі:

    config.py      — читає та перевіряє config.yaml
    models.py      — опис одного оголошення (клас Listing)
    scrapers/      — по одному модулю на кожен сайт (спільний базовий клас)
    filters.py     — залишає лише оголошення, що відповідають критеріям
    database.py    — зберігає оголошення в SQLite, відсіює вже відомі
    notifier.py    — надсилає сповіщення (у термінал та/або email)
    runner.py      — з'єднує все докупи в один робочий цикл
"""

__version__ = "0.1.0"

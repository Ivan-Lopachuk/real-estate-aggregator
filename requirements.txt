# Зовнішні бібліотеки, потрібні для роботи програми.
# Встановлення:  pip install -r requirements.txt

requests>=2.31       # HTTP-запити до сайтів
PyYAML>=6.0          # читання config.yaml
beautifulsoup4>=4.12 # розбір HTML-карток оголошень (Immovlan)

# Усе інше (sqlite3, smtplib, email, argparse, logging) входить
# до стандартної бібліотеки Python і не потребує встановлення.
